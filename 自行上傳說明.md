# 影片去浮水印大師｜自行上傳說明

## 重要提醒

這不是只有 HTML 的靜態網站。影片上傳、浮水印偵測、FFmpeg 處理與下載都需要 Python 後端。

- `web/index.html`、`web/styles.css`、`web/app.js`：網頁介面。
- `api.py`、`src/`：影片處理後端。
- `Dockerfile`：安裝 Python、FFmpeg 並啟動網站。
- `render.yaml`：Render 免費方案設定。

只把 `index.html` 上傳到一般靜態空間，畫面可以顯示，但影片處理功能不會運作。

## 建議上傳方式

1. 解壓縮完整部署包。
2. 將解壓縮後的所有檔案上傳到一個 GitHub Repository。
3. 登入 Render，選擇 **New → Blueprint**。
4. 連接該 GitHub Repository。
5. 確認方案顯示為 **Free**，再開始部署。
6. 部署完成後，Render 會提供一個 `onrender.com` 網址。

## 使用限制

- 支援 MP4、MOV、AVI。
- 最長 60 秒。
- 最高 1920×1080。
- 單檔最大 200 MB。
- 僅能處理你擁有或已獲授權的影片。
